services:
  - name: php
    buildCommand: docker build -t my-laravel-app .
    startCommand: docker run my-laravel-app

  - name: nginx
    buildCommand: docker build -t my-nginx -f Dockerfile.nginx .
    startCommand: docker run -p 8080:80 my-nginx
